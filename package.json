{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --dir website-engine/frontend dev",
    "dev:backend": "cd website-engine/backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "preinstall": "node -e \"const fs = require('fs'); try { fs.unlinkSync('package-lock.json'); } catch(e) {} try { fs.unlinkSync('yarn.lock'); } catch(e) {} const agent = process.env.npm_config_user_agent || ''; if (!agent.includes('pnpm')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@esbuild/win32-x64": "^0.27.3",
    "@rollup/rollup-win32-x64-msvc": "^4.62.2",
    "@tailwindcss/oxide-win32-x64-msvc": "^4.3.1",
    "lightningcss-win32-x64-msvc": "^1.32.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
